{
  "name": "paisaflow-vault",
  "version": "1.0.0",
  "description": "Premium Private Ledger & Vault",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required for static assets'",
    "cap-sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "capacitor-native-biometric": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
